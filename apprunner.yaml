version: 1.0
runtime: nodejs18
run:
  command: npm install && npm run build && npm install -g serve && serve -s dist -l 3000
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
