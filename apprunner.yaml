version: 1.0
runtime: NODEJS_18
build:
  commands:
    - npm install
    - npm run build
run:
  # Use an array for the start command so App Runner parses it reliably
  command:
    - npx
    - serve
    - -s
    - dist
    - -l
    - "3000"
  port: 3000
  environment:
    NODE_ENV: production
